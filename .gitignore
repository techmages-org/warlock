# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
.env
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg

# SQLite / Warlock runtime data
warlock.db
warlock.db-*
*.sqlite
*.sqlite3
/data/

# Node / Vite
node_modules/
web/dist/
*.log
npm-debug.log*
.vite/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Secrets
.env.local
.env.*.local
secrets.yaml

# Local ops docs (build playbooks, handoffs, orchestration logs) — contain creds, never push
ops/
HANDOFF.md
ORCHESTRATION.md
# TUI agent provider key (Z.AI / OpenAI-compatible) — operator-local, never push
agent.env
# Local reference assets (SDR Big Book PDF, device dumps) — operator-local, keep out of git history
assets/
# Vendored conformance oracle (AAR reference verifier) — cloned locally, never push
vendor/
